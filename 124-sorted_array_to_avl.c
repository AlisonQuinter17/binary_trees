#include "binary_trees.h"
/**
 * sorted_array_to_avl - ?
 * @array: ?
 * @size: ?
 * Return: ?
 */
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	(void) array;
	(void) size;
	return (NULL);
}
